# TASK — 실적ON

> 작업 체크리스트 · v1.0
> 규칙: 한 번에 하나씩. 완료 조건을 충족해야 다음으로 넘어간다.

---

## Phase 0 — 기반

### T-001 프로젝트 초기화
- [ ] Next.js 14 App Router + TypeScript strict + Tailwind
- [ ] shadcn/ui 초기화
- [ ] Dexie, zustand, dexie-react-hooks 설치
- [ ] Vitest + Testing Library 설정
- [ ] Pretendard next/font self-host
- [ ] TRD 11절 디렉터리 구조 생성

**완료 조건** `npm run dev` 실행, `npm run test` 통과(0건), `tsc --noEmit` 무오류

### T-002 디자인 토큰
- [ ] globals.css에 CSS 변수 12종 정의
- [ ] tailwind.config.ts theme.extend.colors 매핑
- [ ] 등폭 숫자 유틸 클래스 (`tabular-nums`)

**완료 조건** 임시 페이지에서 토큰 색상 12종이 정확히 렌더

### T-003 타입 정의
- [ ] IssuerKey, CategoryName, Tier, CategoryRate
- [ ] Card, Transaction, Unrecognized
- [ ] Settings, ParseStats
- [ ] ParseResult 판별 유니온, ParseFailReason 리터럴 유니온
- [ ] 금지 필드(카드번호 전체·CVC·비밀번호) 부재 확인

**완료 조건** ERD.md 2절 필드와 1:1 대응, `tsc --noEmit` 통과

### T-004 상수 사전
- [ ] `parser/rules.ts` — 카드사 13종, 매칭 우선순위 고정, `etc` 최후
- [ ] `parser/categories.ts` — 업종 15종, 좁은 범위 우선
- [ ] `classify()` 구현
- [ ] `DEFAULT_EXCLUDE` 3종 export

**완료 조건** `classify('GS25 역삼점') === '편의점'`, `classify('이마트 성수점') === '마트/식료품'`

### T-005 Dexie 스키마
- [ ] 4개 테이블 + 인덱스, 복합 인덱스 `[cardId+date]` 포함
- [ ] CRUD 헬퍼
- [ ] 무결성 규칙 8종 강제
- [ ] `cleanup()` — raw 12개월 정리, unrecognized 30건 제한
- [ ] `migrations.ts` + 업그레이드 전 스냅샷 훅

**완료 조건** 카드 삭제 시 거래가 보존되고 `cardId`만 null이 되는 것을 테스트로 확인

---

## Phase 1 — 파싱 엔진 ★

### T-101 문자 픽스처 수집
- [ ] 8개 카드사 × 5형식 = 40건 이상
- [ ] 형식: 다중행+누적 / 단일행 / 할부 / 취소 / 시각없음
- [ ] 반려 케이스 10건 (청구·이벤트·한도상향 등)
- [ ] 이름·카드번호 마스킹

**완료 조건** `__fixtures__/sms.ts`에 50건 이상, 실제 개인정보 0건

> ⚠️ 이 작업이 Phase 1의 병목이다. 첫날에 확보 가능성을 판정하고, 부족하면 카드사 범위를 줄여 출시한다.

### T-102 파이프라인 골격
- [ ] 정규화 (접두 제거, 공백 정리)
- [ ] 안내 문자 조기 반려
- [ ] 카드사 식별
- [ ] 날짜/시각 추출 + 전년도 보정
- [ ] 할부 개월 추출
- [ ] 취소 판정 + 금액 부호 반전

**완료 조건** 픽스처 50%가 issuer·date까지 정확

### T-103 금액 추출 ★
- [ ] 전역 정규식으로 모든 `N원` 수집
- [ ] 선행 10자에 `누적|누계|잔액|잔여|한도|포인트|합계|총|적립|월` 있으면 제외
- [ ] 제외된 첫 값을 `cumulative`로 보관
- [ ] 결제금액 후보 없으면 반려

**완료 조건** 누적금액 오인식 0건. 이 항목이 실패하면 다음으로 넘어가지 않는다

### T-104 가맹점 추출
- [ ] 3단 폴백 패턴
- [ ] 노이즈 제거 (`승인`·`취소`·`일시불`·`님`·금액 표기)
- [ ] 26자 절사
- [ ] 2자 미만 시 `needsReview: true` + `'가맹점 미상'`, 거래는 유지

**완료 조건** 픽스처 가맹점 추출률 90% 이상

### T-105 다건 분리
- [ ] `splitMessages()` — 빈 줄 분리
- [ ] 한 블록에 `[Web발신]` 다중 출현 시 재분리

**완료 조건** 5건 연결 문자열이 정확히 5건으로 분리

### T-106 골든 테스트 ★
- [ ] 50건 전 필드 검증 (issuer, amount, last4, date, merchant, category, installment, canceled)
- [ ] 반려 케이스 10건 사유까지 검증
- [ ] 파싱 1건 5ms 이내 벤치

**완료 조건 (Phase 1 종료 게이트)**
- 골든 케이스 100% 통과
- `parseSMS`가 순수 함수 (DB 접근·부수효과 없음)
- 테스트를 느슨하게 고쳐 통과시킨 항목 0건

---

## Phase 2 — 도메인 엔진

### T-201 주기 계산
- [ ] `cycleRange(now, startDay)`
- [ ] 29~31일 시작일의 말일 보정
- [ ] `daysLeft()`

**완료 조건** 2월 31일 시작 케이스 통과

### T-202 실적 계산
- [ ] `isExcluded()` 3단 우선순위
- [ ] `computePerformance()` — sum, excluded, cur, next, top, progress, overflow, benefit, nextGain
- [ ] `cardId === null` 거래 제외
- [ ] 취소 거래 음수 반영

**완료 조건** 구간 경계 정확 일치(`sum === min`) 시 달성으로 판정

### T-203 최적화 엔진
- [ ] 미달 카드 정렬 → 타깃 선정
- [ ] `over` / `fill-need` / `ok` 3유형 생성
- [ ] 도달 가능성 판정, 계수 상수 분리
- [ ] 정렬 (초과 최상단 → 이득 내림차순)

**완료 조건** 모든 카드가 최고 구간 초과인 경우에도 오류 없이 대체 카드 없음 메시지 생성

### T-204 분석 계산
- [ ] `monthlyTrend()`
- [ ] `categoryBreakdown()`
- [ ] `yearlyOutlook()` — 연회비 차감

**완료 조건** 거래 0건일 때 0으로 나누기 없음

### T-205 도메인 테스트
- [ ] 경계 8종: 구간 정확 일치 / 취소로 하락 / 무실적 카드 / 2월 31일 / 대체카드 없음 / 거래 0건 / cardId null 혼재 / 남은날 0

**완료 조건** 커버리지 90% 이상

---

## Phase 3 — 화면

### T-301 UI 기본
- [ ] Button 4종 + sm
- [ ] Sheet, Toggle(`role="switch"`), Chip, Toast, Field
- [ ] Money 컴포넌트 (전 화면 금액 표시는 반드시 경유)
- [ ] 터치 타깃 44px, focus-visible, reduced-motion

**완료 조건** 키보드만으로 전 컴포넌트 조작 가능

### T-302 CardGauge ★
- [ ] 카드 형태 컨테이너 + 차오름 배경
- [ ] 실적 구간 세로 눈금 + 등폭 라벨
- [ ] 3상태(미달/달성/초과) 색·배지
- [ ] 상태 문구 4종 + 실적 제외 표기
- [ ] 차오름 애니메이션 0.5s, 탭 시 scale(.988)

**완료 조건** 목 데이터 3상태가 의도대로 렌더. 형광 배경 위 텍스트 대비비 AA

### T-303 대시보드
- [ ] HeroPick (권장 카드 + 이유 + 3열 통계 + 장식)
- [ ] CardGauge 목록
- [ ] 최근 결제 6건
- [ ] 상단 주기·D-N 표시
- [ ] 빈 상태 + 샘플 데이터(카드 3장·거래 29건)

**완료 조건** 카드 0장에서 [샘플 데이터로 둘러보기] 한 번으로 전 화면이 채워짐

### T-304 내 카드 — 목록·에디터
- [ ] CardList (실적 바 + 조건 4줄)
- [ ] CardEditor (기본정보 / 구간 행 추가·삭제 / 제외 업종 칩 / 적립률 그리드)
- [ ] 저장 시 tiers 정렬, 빈 라벨 자동 생성
- [ ] **저장 직후 `cardId === null` 거래 소급 매칭**

**완료 조건** 문자를 먼저 넣고 카드를 나중에 등록해도 실적이 채워짐

### T-305 내 카드 — 템플릿
- [ ] 5종 템플릿
- [ ] 예시 고지 문구 노출

**완료 조건** 템플릿 선택 후 [내 카드]로 이동하고 편집 유도 토스트 표시

### T-306 문자분석 — 수집·통계
- [ ] 토글 3종 + 각 설명 문구
- [ ] 분석 시도 / 성공 / 정확도 3열
- [ ] textarea + [분석해서 저장] + [붙여넣기]
- [ ] 결과 토스트 (저장/미인식/중복 건수)

**완료 조건** 5건 일괄 붙여넣기가 5건으로 저장

### T-307 문자분석 — 구제·편집
- [ ] 미인식 목록 (사유 + 원문 180자 + 직접입력/삭제)
- [ ] 직접 입력 시트, 가맹점 입력 시 업종 실시간 자동 선택
- [ ] 거래 목록 40건
- [ ] 거래 편집 시트 (카드 지정 / 업종 / 실적 반영 3택 / 원문 보기 / 삭제)

**완료 조건** 파싱 실패한 문자가 직접 입력으로 실적에 반영됨

### T-308 최적화
- [ ] 상단 합계 요약
- [ ] 조치 목록 (유형별 세로선 색 + 제목 + 본문 + 이득 배지)
- [ ] 조치 문구에 구체 금액·카드명 포함
- [ ] **"무리라면 이번 주기는 접고 다른 카드에 집중하세요" 문구 포함**
- [ ] 업종별 최고 적립률 카드 표

**완료 조건** 초과 카드가 있을 때 대체 카드명과 필요 금액이 문장에 나타남

### T-309 분석
- [ ] 월별 막대 6개월, 이번 달 강조
- [ ] 업종별 수평 바 + 추천 카드 병기
- [ ] 연간 전망 3행
- [ ] 예측 가정 고지 문구

**완료 조건** 거래 0건에서 빈 상태 메시지 표시, 오류 없음

---

## Phase 4 — PWA·수집

### T-401 PWA 기본
- [ ] manifest.json (share_target GET 포함)
- [ ] 아이콘 192/512 + maskable
- [ ] sw.js 앱 셸 캐시, 구버전 정리
- [ ] `viewport-fit=cover` + safe-area

**완료 조건** Lighthouse PWA 100점, 설치 프롬프트 표시

### T-402 공유 시트 연동
- [ ] `shareTarget.ts` — `?text=` 파싱 → ingest
- [ ] 처리 후 `history.replaceState`로 쿼리 제거
- [ ] sw.js fetch 핸들러에서 공유 요청을 캐시된 index로 응답

**완료 조건 (실기기 필수)** Android 실기기에서 문자 공유 → 저장 성공, 오프라인에서도 동작

### T-403 클립보드
- [ ] `visibilitychange` 훅, 설정 ON일 때만 동작
- [ ] 직전 텍스트 캐시로 중복 팝업 방지
- [ ] 파싱 성공 시에만 저장 확인, 실패 텍스트 즉시 폐기
- [ ] Safari 거부 시 조용히 폴백

**완료 조건** iOS Safari에서 오류·팝업 없이 무시됨

### T-404 백업
- [ ] `exportAll()` → JSON 다운로드, 민감정보 경고
- [ ] `importAll()` → app/schemaVersion 검증 → 병합/덮어쓰기
- [ ] `lastBackupAt` 기록, 30일 경과 배너
- [ ] 전체 삭제 (2단계 확인)

**완료 조건** 내보내기 → 전체 삭제 → 가져오기 왕복이 무손실

---

## Phase 5 — 다듬기·출시

### T-501 보안 점검
- [ ] CSP 헤더 (`connect-src 'self'`)
- [ ] `dangerouslySetInnerHTML` 사용처 0건
- [ ] 외부 스크립트·애널리틱스·CDN 의존 제거
- [ ] 에러 바운더리에 결제 정보 미노출
- [ ] 진단 정보 복사 시 금액·가맹점 마스킹

**완료 조건** DevTools 네트워크 탭에서 최초 로드 이후 외부 요청 0건

### T-502 성능
- [ ] 실적 계산 메모이제이션
- [ ] 거래 5,000건 시드로 재계산 100ms 이내
- [ ] 문자 50건 일괄 500ms 이내
- [ ] 번들 gzip 180KB 이내

**완료 조건** TRD 8절 전 항목 달성. 측정 → 개선 → 재측정 수치 기록

### T-503 접근성
- [ ] 대비비 WCAG AA (형광 배경 위 텍스트 포함)
- [ ] 전 인터랙티브 요소 focus-visible
- [ ] 토글 `role="switch"` + `aria-checked`
- [ ] 금액 aria-label에 "원" 포함
- [ ] reduced-motion 동작

**완료 조건** axe 자동 검사 위반 0건

### T-504 카피 점검
- [ ] 시스템 용어 대신 사용자 언어
- [ ] 버튼 문구와 결과 토스트 동사 일치
- [ ] 절감액을 확정 사실처럼 표현한 곳 없음
- [ ] 빈 상태·오류 문구가 다음 행동을 알려줌

**완료 조건** 전 화면 문구 1회 통독 후 수정 완료

### T-505 최종 검증
- [ ] 주기 경계 리셋 (시스템 시각 조작)
- [ ] 카드 삭제 후 거래 보존
- [ ] 오프라인 전 기능
- [ ] Android 실기기 설치·동작
- [ ] iOS 실기기 설치·동작
- [ ] 본인이 실제로 3일간 사용

**완료 조건 (출시 게이트)** PLAN.md 7절 출시 기준 8개 전부 통과

### T-506 배포
- [ ] Vercel 프로덕션 배포
- [ ] 커스텀 도메인 + HTTPS
- [ ] GUIDE.md를 앱 내 도움말로 연결
- [ ] 피드백 수신 경로 확보 (미인식 문자 제보용)

---

## 출시 후 상시 작업

### T-901 파서 정확도 관리 (주간)
- [ ] 제보된 미인식 원문 수집
- [ ] PROMPT.md 부록 A로 진단·수정
- [ ] 골든 케이스에 추가
- [ ] 기존 테스트 전체 통과 확인

> 기존 테스트를 느슨하게 고쳐 통과시키지 않는다.

### T-902 카드 조건 최신화 (분기)
- [ ] 주요 카드사 정책 변경 확인
- [ ] 템플릿 값 갱신
- [ ] 6개월 미수정 카드 재확인 안내 동작 확인

---

## 진행 현황

| Phase | 작업 수 | 완료 | 게이트 통과 |
|---|:--:|:--:|:--:|
| 0 기반 | 5 | 0 | ☐ |
| 1 파싱 엔진 | 6 | 0 | ☐ |
| 2 도메인 엔진 | 5 | 0 | ☐ |
| 3 화면 | 9 | 0 | ☐ |
| 4 PWA·수집 | 4 | 0 | ☐ |
| 5 다듬기·출시 | 6 | 0 | ☐ |
| **합계** | **35** | **0** | |

---

## 잘라낼 수 있는 작업 (일정 지연 시)

우선순위 낮은 순서:
1. T-309 분석 화면의 연간 전망 → v1.1
2. T-304의 업종별 적립률 입력 + T-308의 업종별 카드 표 → v1.1
3. T-403 클립보드 자동 감지 → 수동 붙여넣기만으로 출시

**자를 수 없는 작업**: T-106(골든 테스트), T-307(미인식 구제), T-308(조치 문구), T-404(백업)
