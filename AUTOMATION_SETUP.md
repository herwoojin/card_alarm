# 문자 자동 수집 설정 — 문자가 오면 자동으로 앱에 넣기

> 결제 문자가 도착하면 **한 번의 설정으로 자동 저장**되게 하는 방법입니다.
> 앱 주소: `https://25f.netlify.app` (배포 도메인 기준. 바뀌면 아래 주소도 함께 바꾸세요)

---

## 어떻게 동작하나 (원리)

웹앱은 백그라운드에서 문자를 스스로 읽을 수 없습니다(브라우저 제약). 그래서 **폰의 자동화 도구가 문자 도착을 감지해, 그 문자 내용을 담은 주소를 여는** 방식으로 자동화합니다.

```
문자 도착 → (폰 자동화) → https://25f.netlify.app/?text=<문자내용>&close=1 열기
          → 앱이 자동으로 파싱·저장 → 완료 화면 표시 (close=1이면 탭 자동 닫기 시도)
```

- **서버가 필요 없습니다.** 결제 문자는 앱(내 기기)에서만 처리됩니다.
- `&close=1` 을 붙이면 처리 후 탭을 닫으려 시도합니다(도구/브라우저에 따라 동작).
- 같은 문자를 두 번 열어도 **중복 저장 안 됨**(중복 건너뛰기 기본 켜짐).

> 정확한 복사용 주소는 앱의 **설정 → 문자 자동 수집**에서 “주소 복사”로 가져올 수 있습니다.

## 두 가지 방법

| | 방법 A — 앱 열기 (`?text=`) | 방법 B — 클라우드 웹훅 (권장·무료) |
|---|---|---|
| 서버 | 필요 없음 | Netlify Function(무료) + RTDB(무료) |
| 앱이 닫혀 있을 때 | 안 됨(열려야 함) | **됨** — 다음에 열 때 자동 저장 |
| 화면 | 문자마다 앱이 잠깐 뜸 | 조용히 저장(앱 안 뜸) |
| Make/Zapier/n8n | 불가 | **가능** |
| 준비 | 없음 | 로그인 + `FIREBASE_SETUP.md` 5단계 |

- **방법 A** 주소: `https://25f.netlify.app/?text=[문자내용]&close=1`
- **방법 B** 주소(로그인 후 **설정 → 문자 자동 수신(웹훅)** 에서 복사, 개인 토큰 포함):
  `https://25f.netlify.app/.netlify/functions/ingest?token=<내토큰>&text=[문자내용]`

아래 레시피에서 “주소” 자리에 A 또는 B를 넣으면 됩니다. **B를 권장**합니다(앱이 닫혀 있어도 받고, 화면도 안 뜸).

---

## 📱 아이폰 — 단축어(Shortcuts) 자동화

1. **단축어** 앱 → 아래 탭 **자동화** → **+** → **개인용 자동화 만들기**
2. **메시지** 선택 → **보낸 사람**에 카드사 문자 번호 입력(예: 15447200 등) → 다음
3. **즉시 실행**(Run Immediately) 켜기 → “실행 전에 묻기” 끄기
4. **동작 추가**로 아래 순서 구성:
   1. **입력에서 텍스트 가져오기**(Get Text from Input) — 입력 = “단축어 입력”(받은 메시지)
   2. **텍스트 인코딩**(URL Encode) — 위 텍스트를 URL 인코딩
   3. **텍스트**(Text) 액션: `https://25f.netlify.app/?text=` + (인코딩된 텍스트) + `&close=1`
   4. **URL 열기**(Open URLs) — 위 텍스트 URL 열기
5. 저장.

> **아이폰 저장소 주의**: 자동화의 “URL 열기”는 보통 **Safari**로 엽니다. 그러니 평소에도 **Safari에서** 실적ON을 쓰세요(홈 화면에 설치한 PWA와 Safari는 iOS에서 저장소가 다를 수 있습니다). 홈 화면 설치본만 쓰면 자동으로 들어온 내역이 안 보일 수 있습니다.

---

## 🤖 안드로이드 — MacroDroid (가장 쉬움)

1. **매크로 추가**
2. **트리거**: `수신 SMS`(Received SMS) → 발신자에 카드사 번호(부분 일치 가능)
3. **동작**: `웹페이지 열기`(Open Web Page) → URL:
   ```
   https://25f.netlify.app/?text={sms_message}&close=1
   ```
   - `{sms_message}` 는 MacroDroid의 SMS 매크로 변수(“문자 내용”)입니다. 변수 삽입 버튼(🏷️)으로 넣으세요.
   - 필요하면 URL 인코딩 옵션을 켜세요.
4. 저장 → 활성화.

## 🤖 안드로이드 — Tasker

1. **Profile** → **Event → Phone → Received Text** (발신자에 카드사 번호)
2. 연결 **Task** → **Net → Browse URL**:
   ```
   https://25f.netlify.app/?text=%SMSRB&close=1
   ```
   - `%SMSRB` = 수신 문자 본문. 필요 시 `Variable Convert`로 URL 인코딩 후 사용하면 특수문자가 안전합니다.

> **삼성 “모드 및 루틴(Bixby Routines)”** 은 SMS **본문**을 URL로 넘기는 기능이 제한적입니다. 안드로이드는 **MacroDroid/Tasker** 를 권장합니다.

---

## ☁️ Make / Zapier / n8n (방법 B 웹훅으로 가능)

이제 방법 B 웹훅이 있어 이 도구들도 쓸 수 있습니다. 흐름은:

```
폰에서 문자 → (SMS→웹훅 앱: 예 "SMS Forwarder", 또는 Tasker/MacroDroid의 HTTP 요청)
           → Make/Zapier/n8n 웹훅 수신 → 실적ON 웹훅 호출:
             GET  https://25f.netlify.app/.netlify/functions/ingest?token=<내토큰>&text=<URL인코딩 문자>
             또는 POST (JSON) { "token": "<내토큰>", "text": "<문자>" }
```

- **n8n**: HTTP Request 노드 → Method GET, URL 위 주소(Expression으로 `text`에 문자 삽입). 또는 POST + JSON Body.
- **Zapier**: Webhooks by Zapier → **GET**(또는 POST) → URL/쿼리에 `token`, `text` 매핑.
- **Make**: HTTP → “Make a request” → GET/POST, `token`·`text` 지정.

> POST JSON 을 쓰면 URL 인코딩 걱정 없이 문자 원문을 그대로 넣을 수 있어 가장 안전합니다.

---

## 참고

- **완전 무화면**: 방법 B(웹훅)는 앱을 띄우지 않고 조용히 저장합니다(앱은 다음에 열 때 또는 열려 있으면 실시간으로 반영). 방법 A는 문자마다 앱이 잠깐 뜹니다.
- **웹훅 주소의 토큰은 개인 비밀**입니다. 남에게 주지 마세요(내 계정 수신함에만 쌓입니다). 노출되면 앱에서 로그아웃 후 재로그인하거나 문의해 토큰을 재발급하세요.
- 어떤 방식이든 **중복 문자는 자동으로 걸러집니다**(같은 카드·금액·시각).

---

## 문제가 생기면

- **자동으로 안 들어와요** → 자동화가 여는 주소가 정확한지(설정 → 문자 자동 수집의 복사 주소), `[문자내용]`/변수가 실제로 치환되는지 확인.
- **금액이 이상해요 / 카드사를 못 찾아요** → 그 문자 형식을 알려주시면 파서 규칙을 보강합니다. 미인식 문자는 앱에 남으니 [직접 입력]으로도 구제됩니다.
- **아이폰에서 안 보여요** → Safari에서 쓰는지 확인(위 저장소 주의 참고).
