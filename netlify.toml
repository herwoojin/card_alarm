# 실적ON — Netlify 배포 설정
# Next.js 정적 내보내기(output: 'export') → out/ 를 그대로 서빙한다(서버리스 함수 없음).
# 보안 헤더는 public/_headers → out/_headers 로 복사되어 적용된다.

[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "22"

# SPA 폴백 없이 정적 파일을 그대로 서빙한다. (해시 라우팅/탭은 클라이언트가 처리)
